@model IEnumerable<Fiap.Api.MonitoramentoAmbiental.Model.LeituraModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Valor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DataHora)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Sensor)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Valor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DataHora)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Sensor.Localizacao)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.LeituraId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.LeituraId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.LeituraId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
